#include<stdio.h>
void main()
{
    int l,b;
    float perimeter;
    printf("enter the values of l,b");
    scanf("%d%d",&l,&b);
    perimeter=2*(l+b);
    printf("the perimeter is: %f" ,perimeter);
}
